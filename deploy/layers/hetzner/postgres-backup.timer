# Postgres backup timer — daily at 03:00 UTC. Enable with:
#   systemctl enable --now postgres-backup.timer
[Unit]
Description=Daily qm Postgres backup (restic)

[Timer]
OnCalendar=*-*-* 03:00:00 UTC
Persistent=true

[Install]
WantedBy=timers.target
